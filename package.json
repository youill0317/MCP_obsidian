{
  "name": "markdown-explorer-mcp",
  "version": "5.0.0",
  "description": "Markdown Explorer MCP server for markdown discovery, reading, and link analysis",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "npm run build && node --test --test-isolation=none dist/utils.test.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ignore": "^7.0.5",
    "yaml": "^2.8.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "typescript": "^5.7.0"
  }
}
